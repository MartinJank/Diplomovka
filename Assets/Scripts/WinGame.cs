using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinGame : MonoBehaviour
{
    // Start is called before the first frame update
    // stop the game after some days
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
